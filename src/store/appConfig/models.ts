export type ConfigCSSDefaultType = Readonly<{
    componentBgColor: string,
    componentTextColor: string,
    componentTitleColor: string
}>;

export type ConfigStatusDefaultType = Readonly<{
    isShowMain: boolean,
    isPageNum: number,
}>;