import {
    ConfigCSSDefaultType,
    ConfigStatusDefaultType
} from './models';

export const defaultCSSState: ConfigCSSDefaultType = {
    componentBgColor: 'white',
    componentTextColor: 'black',
    componentTitleColor: 'red'
};

export const defaultStatusState: ConfigStatusDefaultType = {
    isShowMain: true,
    isPageNum: 0,
};