import React, { useEffect, useState } from 'react';

import './Skill.scss';

type SkillPropsType = {};
type SkillStateType = {};


const Skill = (props: SkillPropsType) => {
    return (
        <div id='Skills'></div>
    );
};

export default Skill;