import * as React from 'react';

// why not using React.Component?
// => i want to use this component as loading page for loadable
//    loadable loading component style is this

const Loading = () => {
    return (
        <div id='loading'>
            Loading...
        </div>
    );
  };

export default Loading;
